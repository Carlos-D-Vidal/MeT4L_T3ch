<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listagem de Produtos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0c0c0c;
            margin: 0;
            padding: 20px;
            color: white;
        }
        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: white;
        }
        .client-list {
            width: 90%;
            max-width: 1000px;
            margin: 0 auto;
            background-color: rgb(51, 51, 51);
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            overflow-x: auto;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid #ddd;
            white-space: nowrap;
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
        button {
            padding: 6px 12px;
            margin: 2px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
        }
        .btn-excluir {
            background-color: #f44336;
            color: white;
        }
        .btn-excluir:hover {
            background-color: #e53935;
        }
        .btn-promocao {
            background-color: #008CBA;
            color: white;
        }
        .btn-promocao:hover {
            background-color: #007bb5;
        }
        @media (max-width: 768px) {
            .client-list {
                width: 100%;
            }
            table {
                display: block;
                overflow-x: auto;
                white-space: nowrap;
            }
            th, td {
                padding: 8px;
                font-size: 14px;
            }
            button {
                font-size: 12px;
                padding: 5px 10px;
            }
        }
        @media (max-width: 480px) {
            th, td {
                padding: 6px;
                font-size: 12px;
            }
            button {
                display: block;
                width: 100%;
                margin: 5px 0;
            }
        }
    </style>
</head>
<body>
    <%- include('../../public/menu.html') %>

    <h2>Listagem de Produtos</h2>

    <div class="item-list">
        <table>
            <thead>
                <tr>
                    <th>Descrição</th>
                    <th>Preço</th>
                    <th>Estoque</th>
                    <th>Código</th>
                    <th>Código de Barras</th>
                    <th>Categoria</th>
                    <th>Registra Comissão</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < item.length;i ++) {%>
                    <tr>
                        <td><%= item[i].desc %></td>
                        <td><%= item[i].preco %></td>
                        <td><%= item[i].quantidade %></td>
                        <td><%= item[i].codigo %></td>
                        <td><%= item[i].codigo_barra %></td>
                        <td><%= item[i].id_categoria %></td>
                        <td><%= item[i].comissao %></td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>

</body>
</html>
