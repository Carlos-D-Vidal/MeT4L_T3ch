<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Produto</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #0c0c0c;
            padding: 20px;
            box-sizing: border-box;
            padding-top: 60px;
        }
        .form-container {
            background-color: rgb(51, 51, 51);
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }
        h2 {
            text-align: center;
            margin-bottom: 20px;
        }
        input, select, textarea {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 4px;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }
        button {
            width: 100%;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
        
        @media (max-width: 480px) {
            .form-container {
                padding: 20px;
            }
            input, select, textarea {
                padding: 8px;
                margin: 8px 0;
            }
            button {
                padding: 8px;
            }
        }
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background-color: #333;
            padding: 10px;
            text-align: center;
            color: white;
        }
    </style>
</head>
<body>
    <%- include('../../public/menu.html') %>
    <div class="form-container">
        <h2>Cadastro de Produto</h2>
        <form action="/usuario/cadastro_produto" method="post">
            <label for="descricao">Descrição</label>
            <textarea id="descricao" value="<%= dados.descricao %>" name="descricao" placeholder="Descrição do produto" rows="4" required></textarea>

            <label for="preco">Preço</label>
            <input type="number" id="preco" value="<%= dados.preco %>" name="preco" placeholder="Preço" step="0.01" required>

            <label for="quantidade">Quantidade em Estoque</label>
            <input type="number" id="quantidade" value="<%= dados.quant %>" name="quant" placeholder="Quantidade em estoque" required>

            <label for="categoria">Categoria</label>
            <select name="id_categoria">
                <% for (let i = 0; i< categoria.length; i++){ %>
                    <option value="<%= categoria[i].id_categoria %>"><%= categoria[i].nome_categoria %></option>    
                <% }  %>   
            </select>
            <br><br>

            <label for="comissao">Comissão</label>
            <select id="comissao"  value="<%= dados.comissao %>" name="comissao" required>
                <option value="1" selected>Sim</option>
                <option value="0">Não</option>
            </select>
            <label for="status">Status</label>
            <select id="status"  value="<%= dados.status %>" name="status" required>
                <option value="1" selected>Ativo</option>
                <option value="0">Inativo</option>
            </select>

            <button type="submit">Cadastrar Produto</button>
        </form>
    </div>
</body>
</html>